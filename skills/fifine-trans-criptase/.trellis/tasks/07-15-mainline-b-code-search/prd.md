# 阶段4c 主线B 代码检索新增

## Goal

对应 Goal.md 10.3节：lib/code-search/{ignore,walker,indexer,exact-search,semantic-search,hybrid-search,security}.mjs + mcp/tools/{query,index,status,read,config-check}.mjs + Mistral embedding 接入验证(第13.3节)。

## Requirements

- TBD

## Acceptance Criteria

- [ ] TBD

## Notes

- Keep `prd.md` focused on requirements, constraints, and acceptance criteria.
- Lightweight tasks can remain PRD-only.
- For complex tasks, add `design.md` for technical design and `implement.md` for execution planning before `task.py start`.
